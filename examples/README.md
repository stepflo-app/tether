# tether examples

One runnable example ships in v0.1:

- **[`draft-followup/`](./draft-followup/)** — Meeting transcript → agent drafts follow-up email → Slack approval → send.

Four directories below are roadmap placeholders for v0.1.x. They contain a README only:

- `suggest-triage/` — coming soon
- `act-stale-issues/` — coming soon
- `suggest-pr-reviewer/` — coming soon
- `draft-changelog/` — coming soon

Each future example will follow the same shape as `draft-followup`: a single Node process, BYO LLM, one channel adapter, and a short README runbook.
